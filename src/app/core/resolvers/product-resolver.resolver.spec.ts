import { ProductResolver } from './product-resolver.resolver';

describe('ProductResolver', () => {
  it('should create an instance', () => {
    expect(new ProductResolver()).toBeTruthy();
  });
});
